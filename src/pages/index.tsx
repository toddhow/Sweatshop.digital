import GeneralPage from '@components/layout/General';
import type { NextPage } from 'next';

const Home: NextPage = () => {
    return (
        <>
            <GeneralPage />
        </>
    );
};

export default Home;
